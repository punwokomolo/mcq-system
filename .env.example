# Copy this file to .env and fill in your values.
# When both SUPABASE_URL and SUPABASE_KEY are set the server uses Supabase.
# Leave them empty (or unset) to fall back to local NeDB files.

SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_KEY=your-anon-or-service-role-key

PORT=3000
